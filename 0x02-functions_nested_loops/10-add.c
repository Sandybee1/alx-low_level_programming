#include "main.h"
/**
 * add - adds two integers and returns the result
 *
 * @c: The first number
 * @d: The second number
 * Return: the addition of both
 */
int add(int c, int d)
{
	return (c + d);
}
